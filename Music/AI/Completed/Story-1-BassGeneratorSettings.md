# Story 1: Normalize bass generator settings to match drum settings

## Summary
- Updated `BassGeneratorSettings.GetActiveRoles()` to default to `GrooveRoles.Bass` instead of drum roles.

## Tests
- `dotnet build`
