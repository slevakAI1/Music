# Story 4: Create BassOperatorApplicator parity with drums

## Summary
- Switched `BassPhraseGenerator` to use `BassOperatorApplicator` and removed drum applicator reference.
- Kept `BassOperatorApplicator` in the bass operators namespace with drum-parity logic.

## Tests
- `dotnet build`
